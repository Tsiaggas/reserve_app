{
  "name": "reserve-app",
  "version": "1.0.0",
  "description": "Restaurant Reservation Application",
  "scripts": {
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 